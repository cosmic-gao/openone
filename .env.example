# OpenOne 环境变量说明
#
# 自 v0.2.0 起，每个 APP 拥有独立的 .env 文件（Next.js 自动加载）。
# 请参考各 APP 目录下的 .env.example 进行配置：
#
#   apps/shell/.env.example      → Shell APP (端口 3000)
#   apps/auth/.env.example       → Auth APP (端口 3001)
#   apps/admin/.env.example      → Admin APP (端口 3002)
#   apps/permission/.env.example → Permission APP (端口 3003)
#   apps/database/.env.example   → Database APP (端口 3004)
#
# 环境变量由三大核心服务分别管理：
#   Admin APP      → 端口、域名、服务地址
#   Database APP   → 数据库连接URL、Schema配置
#   Permission APP → RBAC权限相关配置
#
# 快速初始化：
#   cd apps/shell && cp .env.example .env
#   cd apps/auth && cp .env.example .env
#   cd apps/admin && cp .env.example .env
#   cd apps/permission && cp .env.example .env
#   cd apps/database && cp .env.example .env

# 以下为全局共享变量（仅供参考，实际值请在各APP .env中配置）
DATABASE_URL=postgresql://postgres:123456@localhost:5432/openone
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_REFRESH_SECRET=your-refresh-secret-key-change-in-production
